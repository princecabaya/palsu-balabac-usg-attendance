window.USG_ATTENDANCE_CONFIG = Object.freeze({
  // Paste the /exec URL from the deployed Google Apps Script web app here.
  // The Control Dashboard also accepts a device-only override during setup.
  apiUrl: "",
  campusName: "Palawan State University – Balabac Campus",
  organizationName: "University Student Government",
});
